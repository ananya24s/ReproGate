"""Coordinates a verification run from queued to terminal state.

Depends on the GitHub, repository analysis, LLM, sandbox, and persistence
interfaces. Workflow decisions live here rather than in the API layer."""
